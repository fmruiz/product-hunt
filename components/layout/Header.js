import React from 'react'
import styled from '@emotion/styled'
import { Search } from '../ui/Search'

export const Header = () => {
    return (
        <header>
            <div>
                <div>
                    <p>P</p>

                    <Search />
                </div>

                <div>

                </div>
            </div>
        </header>
    )
}
